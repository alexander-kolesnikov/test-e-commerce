<p id="notice"><%= notice %></p>

<p>
  <strong>Email:</strong>
  <%= @order.email %>
</p>
<table class="table table-striped table-bordered">
  <tr>
    <td>Product</td>
    <td>Quantity</td>
  </tr>
<% @order.order_details.each do |detail| %>
  <tr>
    <td><%= Product.find(detail.product_id).title%></td>
    <td><%= detail.qty%>
  </tr>
<% end %>
</table>
  
<p>
  <strong>Total:</strong>
  <%= @order.total %>
</p>

<%= link_to 'Back', admin_orders_path %>
